{
  "name": "channel-sync-bot",
  "version": "1.0.0",
  "description": "Sync channels between multiple servers!",
  "main": "index.js",
  "scripts": {
    "build": "npm install",
    "build-prod": "npm install --only=prod",
    "dist": "npm run build-prod && mkdir -p dist && cp -r node_modules dist/ && cp index.js config.js dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kevin Cooper <kevin@kevincooper.io>",
  "license": "MIT",
  "dependencies": {
    "discord.js": "^12.3.1"
  },
  "devDependencies": {
    "eslint": "^7.9.0",
    "eslint-config-standard": "^14.1.1",
    "eslint-plugin-import": "^2.22.0",
    "eslint-plugin-node": "^11.1.0",
    "eslint-plugin-promise": "^4.2.1",
    "eslint-plugin-standard": "^4.0.1"
  }
}
